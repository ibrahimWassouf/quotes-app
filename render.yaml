databases:
  - name: mysite
    databaseName: mysite
    user: mysite

services:
  - type: web
    name: quotes-app
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn quotes.wsgi"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4